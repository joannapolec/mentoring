const person = {
  name: 'Aneta',
  surname: 'Kowalska',
  age: 25,
  height: 1.6,
};

// I want to add the `employed` property which should be set to true to this object. How would I do it below? Do not change the code above.

// ANSWER (BELOW):

// Write the `name` property to the console using `person` variable

// ANSWER (BELOW):

// Use hasOwnProperty() method to determine if the `person` object has a property called `status`

// ANSWER (BELOW):
