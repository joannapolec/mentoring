// This function should accept `length` number and return an array of randomly generated number (the array is of length `length`). Use Math.random().
// For negative value of `length`, return an empty array.
const vector = () => {};

export default vector;
